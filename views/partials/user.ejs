<div id="userControls">
<% if (token || user) { %>
  <p><%- token ? token.displayName : user %> || <a href="/logout">Log Out</a></p>
<% } else { %>
  <a href="<%- this_url %>">Log in with Formbar</a>
<% } %>
</div>
